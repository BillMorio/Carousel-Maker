import { CarouselTemplate } from "../defaultTemplates";
import { theHookTemplate } from "./the-hook";
import { aiAgentsGuideTemplate } from "./ai-agents-guide";
import { modernBusinessTemplate } from "./modern-business";

// Import all template presets
// TODO: Import other templates as they are created

export const allTemplates: CarouselTemplate[] = [
  theHookTemplate,
  aiAgentsGuideTemplate,
  modernBusinessTemplate,
  // Add more templates here
];
